## Stack: Python 3.12

Review idiomatic Python 3.12. Expect type hints throughout, docstrings on public functions
and classes, no global mutable state, and secrets read from environment variables (never
hardcoded). Interface contracts are `from <module> import <symbol>` imports against files
under `src/`.
